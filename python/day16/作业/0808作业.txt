今日作业：
	1.以自己的理解总结为什么会出现循环导入，并用代码举例说明
#1.两个模块都需要对方的数据2.模块被导入一次之后，就不会重新导入3.数据还没有导全,但已经生成了一个名称空间,不会再次生成,也不会再次导入
	2.如何来解决循环导入，并解决第1题中的循环导入问题
#方式一:把将要导入的模块中需要的数据放到import前面
#方式二:用函数包起来
	3.完成housework.py文件，拥有三个功能：sweep、wash、cook，自定义功能实现(函数体)，从文件自执行和作为模块使用两方面，验证三个功能，且两方面使用方式可以共存
	4.导入模块的搜索路径有哪些？它们的优先级是？
#1.内存
#2.内置
#3.当前执行文件的sys.path
	5.现有一个run.py运行文件，与run文件同级目录下有一个pgk文件夹，文件夹下有两个模块m1、m2，m1模块内有功能f1，可以打印字符串"我是m1模块"，
	m2模块内有功能f2，可以打印字符串"我是m2模块"，在run文件中，通过绝对路径方式导入m1模块，验证功能，在模块m1中通过相对路径导入m2模块，验证功能

	6.按照软件开发的目录规范，为VFP(图书管理系统)建立一套规范的目录结构，并总结个目录结构的功能
VFP
|
----bin
        |
        ----start.py
|
----conf
        |
        ----setting.py
|
----db
        |
        ----db.txt
|
----lib
        |
        ----common.py
|
----log
        |
        ----log.txt
|
----core
        |
        ----src.py
|
----Readme

bin整个项目的开始文件,conf是放一些各个模块都需要的变量,db是记录数据,log是日志记录信息,core是核心逻辑
lib放一些模块放自定义的文件Read说明书